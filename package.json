{
  "name": "eng-aserdargun-com",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An open manifesto for AI-native humanoid engineering education.",
  "scripts": {
    "dev:codex": "vite --host 127.0.0.1 --port 4173 --strictPort",
    "build": "vite build",
    "test:unit": "node --test tests/client-behavior.test.mjs tests/manifesto-content.test.mjs tests/project-contract.test.mjs tests/repository-handoff.test.mjs tests/stop-dev.test.mjs",
    "test:e2e": "playwright test",
    "validate:codex": "npm run test:unit && npm run build && node --test tests/dist-artifact.test.mjs && npm run test:e2e && git diff --check",
    "stop:codex": "node scripts/stop-dev.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "vite": "8.2.2"
  },
  "dependencies": {
    "@fontsource-variable/archivo": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0"
  }
}
